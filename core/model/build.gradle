plugins {
    alias(libs.plugins.android.library)
}

android {
    namespace 'com.gustavo.brilhante.model'
    compileSdk 36
    defaultConfig { minSdk 24 }
}

kotlin.jvmToolchain(17)

dependencies {
    testImplementation libs.junit
}
